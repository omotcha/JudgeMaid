# The maximum length of a sentence. This is not maximum of token though it matters, so it should not be set too large.
max_words = 512

# The minimum length of a sentence. This is not minimum of token.
min_words = 96

# If a sentence exceeds the limit then discard it(set it True) or trim it(set it False)
discard_large_sentence = True

# minimal number of samples per class
min_samples_per_class = 10
